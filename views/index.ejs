<%- include('./header.ejs'); %>

    <% for (const film of films) { %>
        <div class="film-card">
            <h3 class="film-title oswald-card-title"><%= film.name %></h3>
            <img src="<%= film.image %>" alt="<%= film.name %> image">    
            <p>Year: <%= film.year %></p>
        </div>
    <% } %>
</main>
</body>